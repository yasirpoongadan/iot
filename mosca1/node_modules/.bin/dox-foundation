#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dox-foundation/bin/dox-foundation" "$@"
  ret=$?
else 
  node  "$basedir/../dox-foundation/bin/dox-foundation" "$@"
  ret=$?
fi
exit $ret
